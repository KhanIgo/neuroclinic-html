+b.b-single-header-row.row
    +b.b-single-header
        +e.left
            +e.left-cnt
                if isClinic
                    include _info-clinic
                if isDoctor
                    include _info-doctor
                if isNews
                    include _info-news
                    
            
        +e.right
            +e.right-cnt
                if rightImgSrc
                    +e.IMG(src=""+ rightImgSrc).img
        
            